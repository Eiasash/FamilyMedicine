# FamilyMedicine (Mishpacha Mega) — Overnight Audit 2026-05-18

**Branch:** `audit/overnight-20260518` (from `origin/main` @ `0de66bc` v1.21.27)
**Scope:** §C profile, audit-fix-deploy. Unattended. Read-only first, then bug/lint/hygiene fixes only. NO push to main, NO deploy, NO PR.
**Repo identity confirmed:** `github.com/Eiasash/FamilyMedicine`, package `mishpacha-mega`, vite base `/FamilyMedicine/` (deployConfigGuard green — NOT the `/InternalMedicine/` fork-bug).

---

## Pipeline status: PASS

| Gate | Result |
|---|---|
| `node scripts/sync-sw-version.cjs` | OK — version 1.21.27, no files modified |
| `npm test` (vitest) | **55 files, 961 tests, all pass** (post-fix) |
| `bash scripts/build.sh` | OK — `dist/sw.js` `CACHE=mishpacha-v1.21.27`, verify-dist-sw OK (19 cached paths) |
| `npx vitest run tests/deployConfigGuard.test.js` | 3/3 pass (vite base + 3-loc version sync) |
| `npm audit` (prod + all) | **0 vulnerabilities** |

`npm run verify` (the canonical gate = sync-sw-version + test + build) is green end-to-end.

---

## Findings by severity

### Critical / High
None. No crashes, silent failures, wrong-output bugs, or leaks found.

### Medium
None actionable in scope.

### Low (fixed)
7 ESLint **errors** at baseline (lint is NOT in `verify` or `ci.yml` — tech-debt, non-CI-blocking). 6 fixed, 1 deliberately left (sibling-shared, see below):

1. `tests/fsrsBoundariesAndBidi.test.js:34` — `no-unused-vars`: unused `beforeAll` import. Verified genuinely unused (no commented-out setup). **Fixed** (dropped from import).
2. `src/core/constants.js:167` — `no-useless-escape` ×2: `\"` inside a single-quoted Hebrew CHANGELOG string. **Fixed** (`\"`→`"`; rendered text byte-identical).
3. `tests/textbookChapters.test.js:50,105,128` — `no-useless-escape` ×3: `\-` at end of character classes `[*—–\-]` / `[—–\-]`. **Fixed** (`\-`→`-`; regex behavior unchanged — `-` positionally safe as last class member). All 961 tests still pass including these two modified test files.

Post-fix lint errors: **7 → 1** (the 1 remaining is the deliberately-untouched shared file).

### Informational / deliberate leaves
- **`shared/install-promo.js:82` `no-empty` error — NOT fixed (deliberate).** `catch {}` for a localStorage quota write. md5 `4ef4dafe83fb9a1828761fa7248ed521` is **byte-identical with InternalMedicine and Geriatrics**, and the workspace canonical lives in `.shared/install-promo.js`. HARD RULES + the cross-repo sibling-shared invariant forbid drifting it from a single-repo unattended audit. Needs a coordinated 3-repo (or `.shared/` source) change — log for Eias.
- **183 ESLint warnings** untouched — overwhelmingly `'e' is defined but never used` in `catch (e) {}` handlers, a pervasive codebase idiom. Chasing them is scope creep with churn risk; not in priority scope.
- **Vite `8.0.11`** in node_modules/lockfile vs MEMORY.md "Workspace Vite 7 baseline" (`^7.3`, dated 2026-05-05). Present in the lockfile at branch point — NOT introduced by this audit. Pipeline (test + build) passes clean on Vite 8. "No major dep bumps" rule → no action; flagged for Eias to confirm intentional vs stale memory.

---

## Audit checks — all clean

- **Version trinity/quartet:** package.json `1.21.27` == APP_VERSION `1.21.27`; `BUILD_HASH='1061q-v1.21.27'` — Q count **1061** verified against `data/questions.json`; sw.js CACHE `mishpacha-v1.21.27`. No existing mismatch. (Not bumped — out of scope.)
- **Data integrity:** 1061 total Qs. Per-tag exactly matches `questionsCountLock.test.js`: 2020=150, 2021-Jun=150, 2022-Jun=150, 2023-Jun=150, 2024-May=100, 2024-Sep=100, 2025-Jun=150, FM-Core=111. All tags in whitelist.
- **Topic coverage:** ti 0..26 all present, min=16 (ti=19), no bucket <5 (no <3). FM-Core Qs carry `t='FM-Core'`.
- **Regression guards:** all in the 55-file / 961-test suite pass (ð-mojibake, Latin-1/Hebrew adjacency, dupes, c-vs-explanation drift, c_accept schema, FSRS boundaries, bidi numerics, SW manifest, deployConfigGuard).
- **Code hygiene greps:** ungated `console.log` in src/ → none (4 found, all `import.meta.env.DEV`-gated). `dir="rtl"` in src/ui/ → none. `TODO|FIXME` in src/ → none. Inline APP_VERSION not from constants.js → none (all `1.21.x` hits are changelog comment markers; runtime imports from `src/core/constants.js`).
- **Secret hygiene:** no hardcoded PAT/API-key/AWS/Slack patterns in committed source. Two `sk-ant-` hits are `placeholder="sk-ant-..."` text in password inputs (correct UX, not secrets). `npm audit` 0 vulns.
- **`shared/fsrs.js`:** md5 `71f9f2d406a1935911e86612439ea58a` (LF-normalized canonical, matches CLAUDE.md `71f9f2d4…`), git blob `89aa3940a942c03201d9d89db02a90665b2910a8` (matches canonical hash-object). **Byte-identical with InternalMedicine + Geriatrics — zero drift.** NOT modified.

---

## CRLF/LF verification

All edits verified non-CRLF-churn: `git diff --stat -w` == `git diff --numstat` (1+1+3 real line changes, no whitespace-only noise). Committed blobs confirmed LF in index (`git ls-files --eol` → `i/lf`; `git show HEAD:… | file -` shows no CRLF terminators). The Windows working-tree CRLF warning is normal `.gitattributes` `* text=auto eol=lf` behavior — index stays LF.

---

## Fix commits

| Commit | Summary |
|---|---|
| `94a886a` | chore(lint): fix no-useless-escape + no-unused-vars (6 errors) |

(Plus this report commit.)

---

## Top 3 items needing Eias's review

1. **`shared/install-promo.js:82` `no-empty`** — sibling-shared (byte-identical FM/IM/Geri, canonical in `.shared/`). Either accept as intentional empty-catch (add `// eslint-disable` or `/* noop */`) in `.shared/` source and propagate, or `catch (_e) {}`. Must be a coordinated cross-repo change, not a single-repo edit.
2. **Vite 8.0.11 vs MEMORY.md "Vite 7 baseline"** — confirm whether the workspace intentionally moved to Vite 8 (then update the memory entry `project_workspace_vite_7_baseline.md`) or whether this is unexpected lockfile drift to investigate across siblings.
3. **183 lint warnings** (catch-handler `'e' unused`) — decide policy: bulk `catch {}` migration (ES2019 optional-catch-binding), an `argsIgnorePattern: '^e$'` eslint rule tweak, or accept as-is. Codebase-wide, needs a single deliberate decision rather than incremental unattended edits.

## Skipped / reverted
- Nothing reverted. No fix broke the pipeline (961/961 green pre- and post-fix).
- Per HARD RULES: stopped after local verify. NO live-URL verification, NO skill-update, NO self-improve/deploy. NO RLS/migration work (none found). The launch prompt's proxy-secret "known fix" was already declined at orchestrator level — ignored entirely, no secret/credential edits made.
- 1 lint error deliberately left (`shared/install-promo.js`, sibling-shared invariant).
