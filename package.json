{
  "name": "mishpacha-mega",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "bash scripts/build.sh",
    "hooks:install": "git config core.hooksPath scripts/git-hooks && echo '✓ pre-commit + pre-push hooks active'",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint src/ shared/ tests/",
    "format": "prettier --write 'src/**/*.{js,css}' 'shared/**/*.js' 'tests/**/*.js'",
    "format:check": "prettier --check 'src/**/*.{js,css}' 'shared/**/*.js' 'tests/**/*.js'",
    "verify": "node scripts/regen_manifest.cjs --check && node scripts/sync-sw-version.cjs && npm test && bash scripts/build.sh",
    "build:vite": "vite build",
    "regen:manifest": "node scripts/regen_manifest.cjs",
    "regen:manifest:check": "node scripts/regen_manifest.cjs --check",
    "check:syllabus-sync": "node scripts/check-syllabus-sync.cjs"
  },
  "devDependencies": {
    "@eslint/js": "^9.28.0",
    "@vitest/coverage-v8": "^4.1.5",
    "acorn": "^8.16.0",
    "eslint": "^10.3.0",
    "globals": "^16.2.0",
    "prettier": "^3.5.3",
    "vite": "^8.0.11",
    "vitest": "^4.1.5",
    "jsdom": "^29.1.1"
  },
  "version": "1.26.18"
}
